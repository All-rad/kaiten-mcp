# Вклад в проект / Contributing

Этот документ описывает базовые процедуры для внесения изменений в репозиторий.

## Политика Pull Request (PR)
- Каждый Pull Request ДОЛЖЕН содержать ровно **один** коммит.
- Commit messages **MUST** быть на английском языке.
- Если после открытия PR требуются дополнительные фиксы, используйте:
  - `git commit --amend` — для изменения последнего коммита
  - `git rebase -i <base>` — для исправления истории перед пушем
- НЕЛЬЗЯ добавлять дополнительные коммиты в PR для последовательных фиксов одной и той же задачи. Перед слиянием PR должен оставаться один аккуратный коммит.

## Как исправить сообщение коммита
```bash
# Изменить сообщение последнего коммита
git commit --amend -m "Your English commit message here"
# Затем force-push в ваш PR branch
git push --force-with-lease
```

## Локальная среда разработки (sdkman / nvm)
**MUST**: используйте `sdkman` для Java и `nvm` для Node.js.

### Рекомендуемые шаги перед началом работы
1. Запустите скрипт проверки окружения:
```bash
./scripts/check-env.sh
```
2. Если скрипт сообщает несоответствие версий, исправьте с помощью:
- Java: `sdk install java <version>` и `sdk use java <version>` (см. https://sdkman.io/)
- Node.js: `nvm install <version>` и `nvm use <version>` (см. https://github.com/nvm-sh/nvm)

## Проверка перед созданием PR
- Убедитесь, что все unit-тесты проходят локально.
- Убедитесь, что PR содержит ровно один коммит и корректное (английское) сообщение.
- Убедитесь, что версия Java и Node соответствуют `docs/quickstart.md`.

## CI
Этот репозиторий содержит workflow, который проверяет:
- что PR содержит ровно один коммит
- что сообщение коммита не содержит кириллицы

Если CI упал по причине политики коммитов — исправьте локально и force-push с `--force-with-lease`.

Спасибо за вклад! 🚀